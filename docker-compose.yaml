name: conductor
services:
  db:
    image: postgres:14.20-alpine
    restart: always
    shm_size: 128mb
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: example
      POSTGRES_DB: conductor
  
  engine-api:
    depends_on:
      - db
    build:
      context: engine/
      dockerfile: src/Conductor.Engine.Api/Dockerfile
    volumes:
      - /home/james/Documents/Conductor:/tmp/conductor
    ports:
      - "8081:8080"
    environment:
      - POSTGRES_HOST=db
      - POSTGRES_PORT=5432
      - POSTGRES_DB=conductor
      - POSTGRES_USER=admin
      - POSTGRES_PASSWORD=example
    develop:
      watch:
        - action: sync+restart
          path: .engine/src/Conductor.Engine.Api
          target: .engine/src/Conductor.Engine.Api
  
  inventory-api:
    depends_on:
      - db
    build:
      context: engine/
      dockerfile: src/Conductor.Inventory.Api/Dockerfile
    ports:
      - "8081:8080"
    environment:
      - POSTGRES_HOST=db
      - POSTGRES_PORT=5432
      - POSTGRES_DB=conductor
      - POSTGRES_USER=admin
      - POSTGRES_PASSWORD=example
    develop:
      watch:
        - action: sync+restart
          path: .engine/src/Conductor.Inventory.Api
          target: .engine/src/Conductor.Inventory.Api
  
  cli:
    depends_on:
      - engine-api
    build:
      context: client/Conductor.Client.Cli
      dockerfile: Dockerfile
    develop:
      watch:
        - action: sync+restart
          path: .client/Conductor.Client.Cli
          target: .client/Conductor.Client.Cli
  
  web:
    depends_on:
      - engine-api
      - inventory-api
    build:
      context: client/Conductor.Client.Web
      dockerfile: Dockerfile
    develop:
      watch:
        - action: sync+restart
          path: .client/Conductor.Client.Web
          target: .client/Conductor.Client.Web