meta {
  name: Update Organisation
  type: http
  seq: 3
}

put {
  url: {{CORE_API_BASE_URL}}/organisations/{{ORGANISATION_ID}}
  body: json
  auth: inherit
}

body:json {
  {
    "name": "Test Organisation (Updated)"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
