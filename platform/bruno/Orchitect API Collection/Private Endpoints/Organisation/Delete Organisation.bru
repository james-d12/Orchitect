meta {
  name: Delete Organisation
  type: http
  seq: 5
}

delete {
  url: {{API_BASE_URL}}/organisations/{{ORGANISATION_ID}}
  body: json
  auth: inherit
}

settings {
  encodeUrl: true
  timeout: 0
}
