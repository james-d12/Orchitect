meta {
  name: Delete Credential
  type: http
  seq: 6
}

delete {
  url: {{CORE_API_BASE_URL}}/credentials/{{CREDENTIAL_ID}}
  body: none
  auth: inherit
}

settings {
  encodeUrl: true
  timeout: 0
}
