meta {
  name: Get All Credentials
  type: http
  seq: 2
}

get {
  url: {{CORE_API_BASE_URL}}/credentials?organisationId={{ORGANISATION_ID}}
  body: none
  auth: inherit
}

params:query {
  organisationId: {{ORGANISATION_ID}}
}

settings {
  encodeUrl: true
  timeout: 0
}
