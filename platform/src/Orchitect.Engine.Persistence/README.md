# Orchitect Persistence
This project contains the Data storage of Orchitect data. It contains the Entity Framework Core code that stores the data
in a database. It is separated from Infrastructure, as Infrastructure is for third party dependencies that we use for 'Drivers'